import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import ShoutoutCustomizer from './ShoutoutCustomizer';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ShoutoutCustomizer />
  </React.StrictMode>
);
