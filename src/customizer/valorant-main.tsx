import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import ValorantCustomizer from './ValorantCustomizer';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <ValorantCustomizer />
  </React.StrictMode>
);
