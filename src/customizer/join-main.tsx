import React from 'react';
import ReactDOM from 'react-dom/client';
import '../index.css';
import JoinCustomizer from './JoinCustomizer';

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <JoinCustomizer />
  </React.StrictMode>
);
